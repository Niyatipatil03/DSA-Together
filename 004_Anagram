sort(a.begin(),a.end());
        sort(b.begin(),b.end());
        for(int i=0;i<a.length()||i<b.length();i++)
        {
        if(a[i]==b[i])
        {
        }
        else
        {
        return false;
        }
        }
        return true;
